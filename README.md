# DV Message Inspector Bot 🤖

Бот для поддержания культуры общения в Telegram-группах через систему реплаев и гибкую систему наказаний.

## Описание 📝

Бот автоматически следит за правилами общения в группе и применяет санкции к нарушителям. Основная задача - поддержание культуры общения через систему реплаев и предотвращение спама.

## Основные возможности 🌟

- Контроль за использованием реплаев:
  - Проверка наличия реплаев в сообщениях
  - Предотвращение двойных ответов на одно сообщение
  - Контроль самоответов (ответов на свои сообщения)
- Предотвращение спама и флуда:
  - Ограничение частоты ответов
  - Контроль длины сообщений
  - Защита от массовых нарушений
- Гибкая система наказаний:
  - Прогрессивная шкала наказаний
  - Поддержка множественных наказаний
  - Прозрачные уведомления
- Адаптивная работа в разных типах групп:
  - Полная функциональность в супергруппах
  - Адаптированные наказания в обычных группах
- Административные функции:
  - Панель управления для модераторов
  - Подробное логирование действий
  - Система уведомлений о нарушениях

## Типы нарушений 🚫

1. **Отсутствие реплая** (`no_reply`):
   - Отправка нескольких сообщений подряд без использования реплая
   - Нарушает структуру обсуждения
   - Затрудняет следование за диалогом

2. **Двойной ответ** (`double_reply`):
   - Повторный ответ на одно и то же сообщение в короткий промежуток времени
   - Создает избыточность в обсуждении
   - Может использоваться для спама

3. **Самоответ** (`self_reply`):
   - Ответ на собственное сообщение раньше установленного времени
   - Нарушает flow диалога
   - Вместо этого рекомендуется редактировать исходное сообщение

## Система наказаний ⚖️

### Типы наказаний:

1. **Предупреждение** (`warning`):
   - Информирование о нарушении
   - Показ количества накопленных нарушений
   - Предупреждение о следующем наказании

2. **Режим "только чтение"** (`read-only`):
   - В супергруппах: временное ограничение прав отправки сообщений
   - В обычных группах: кратковременный бан
   - Длительность настраивается через `mute_duration_seconds`

3. **Исключение** (`kick`):
   - Временное исключение из группы
   - Возможность сразу вернуться
   - Служит как серьезное предупреждение

4. **Временный бан** (`kick+ban`):
   - Исключение с запретом возвращения на определенное время
   - Длительность настраивается через `temp_ban_duration_seconds`
   - Автоматическое разбанивание по истечении срока

5. **Перманентный бан** (`ban`):
   - Полное исключение из группы
   - Запрет на возвращение
   - Применяется за серьезные или систематические нарушения

### Порядок применения наказаний:

1. Отправка уведомления о наказании
2. Задержка для прочтения (`bot_message_delay_seconds`)
3. Применение самого наказания

## Настройка бота ⚙️

### Основные параметры (config.json):

```json
{
  "bot_token": "YOUR_BOT_TOKEN",
  "allowed_groups": [-100123456789],
  "admin_ids": [123456789],
  "admin_chat_id": "-1002465953388_128",

  "message_length_limit": 500,
  "reply_cooldown_seconds": 5,
  "ignore_bot_thread_replies": true,

  "mute_duration_seconds": 3600,
  "temp_ban_duration_seconds": 86400,
  
  "bot_message_delay_seconds": 2,
  
  "delete_bot_messages": true,
  "bot_message_lifetime_seconds": 30,
  "delete_penalty_messages": false,
  "penalty_message_lifetime_seconds": 300
}
```

### Описание параметров:

- `message_length_limit` - Максимальная длина сообщения для проверки правил (в символах)
  - Если длина сообщения превышает это значение, все проверки отключаются
  - Это позволяет писать длинные сообщения (инструкции, документацию и т.д.) без риска нарушений
  - По умолчанию: 500 символов

- `check_reply_cooldown` - Включить/выключить проверку временного интервала между сообщениями
  - Если отключено, временной интервал не учитывается при определении нарушений
  - Если включено, используется значение `reply_cooldown_seconds`
  - По умолчанию: true

- `reply_cooldown_seconds` - Минимальный интервал между сообщениями (в секундах)
  - Используется только если `check_reply_cooldown` включен
  - Определяет минимальное время между:
    * Повторными ответами на одно сообщение
    * Сообщениями без реплая
    * Ответами на свои сообщения
  - По умолчанию: 3600 секунд (1 час)

- `warn_admins` - Предупреждать ли администраторов о нарушениях
  - Если включено, бот будет отправлять уведомления в админ-чат о нарушениях от администраторов
  - Наказания к администраторам не применяются в любом случае
  - По умолчанию: false

- `ignore_bot_thread_replies` - Игнорировать ли ответы на сообщения ботов в тредах
  - По умолчанию: true

- `mute_duration_seconds` - Длительность мута/временного бана в секундах
- `temp_ban_duration_seconds` - Длительность временного бана в секундах
- `bot_message_delay_seconds` - Задержка между уведомлением и применением наказания

### Настройки сообщений бота:
- `delete_bot_messages` - Удалять ли сообщения бота
- `bot_message_lifetime_seconds` - Через сколько удалять сообщения бота
- `delete_penalty_messages` - Удалять ли сообщения о наказаниях
- `penalty_message_lifetime_seconds` - Через сколько удалять сообщения о наказаниях
- `delete_violationg_user_messages` - Удалять ли сообщения пользователей при нарушении правил

### Настройки уведомлений:

```json
"notifications": {
  "violation_rules": false,    // Уведомления о нарушениях правил (no_reply, double_reply, self_reply)
  "mute_applied": false,       // Уведомления о применении мута
  "kick_applied": false,       // Уведомления о кике пользователя
  "kick_ban_applied": false,   // Уведомления о временном бане
  "ban_applied": false,        // Уведомления о постоянном бане
  "official_warning": false    // Официальные предупреждения о нарушениях
}
```

Каждый параметр можно включить (`true`) или выключить (`false`) независимо от других. Это позволяет тонко настроить, какие уведомления будут отправляться в группу.

### Настройки функций:

```json
"features": {
  "violation_counter": false,  // Подсчет нарушений
  "penalties": false          // Система наказаний
}
```

- `violation_counter` - Включает/выключает подсчет нарушений
  - Если выключено, бот не будет вести учет нарушений
  - История нарушений не будет сохраняться
  - Полезно, если нужно только отслеживать нарушения без накопления статистики

- `penalties` - Включает/выключает систему наказаний
  - Если выключено, бот не будет применять никаких санкций
  - Нарушения могут учитываться (если включен `violation_counter`)
  - Уведомления могут отправляться (если включены соответствующие параметры в `notifications`)
  - Полезно, если нужно только мониторить нарушения без применения наказаний

### Примеры конфигурации:

1. Только мониторинг без наказаний:
```json
{
  "features": {
    "violation_counter": true,   // Считаем нарушения
    "penalties": false           // Не применяем наказания
  },
  "notifications": {
    "violation_rules": true,     // Уведомляем о нарушениях
    "mute_applied": false,       // Отключаем уведомления о наказаниях
    "kick_applied": false,
    "kick_ban_applied": false,
    "ban_applied": false,
    "official_warning": true     // Оставляем предупреждения
  }
}
```

2. Тихий режим (только наказания):
```json
{
  "features": {
    "violation_counter": true,   // Считаем нарушения
    "penalties": true           // Применяем наказания
  },
  "notifications": {
    "violation_rules": false,    // Отключаем все уведомления
    "mute_applied": false,
    "kick_applied": false,
    "kick_ban_applied": false,
    "ban_applied": false,
    "official_warning": false
  }
}
```

3. Полностью пассивный режим:
```json
{
  "features": {
    "violation_counter": false,  // Не считаем нарушения
    "penalties": false          // Не применяем наказания
  },
  "notifications": {
    "violation_rules": false,    // Отключаем все уведомления
    "mute_applied": false,
    "kick_applied": false,
    "kick_ban_applied": false,
    "ban_applied": false,
    "official_warning": false
  }
}
```

### Правила нарушений:

```json
"violation_rules": {
  "no_reply": {
    "enabled": true,
    "count_as_violation": true,
    "violations_before_penalty": 1
  },
  "double_reply": {
    "enabled": true,
    "count_as_violation": true,
    "violations_before_penalty": 1
  },
  "self_reply": {
    "enabled": true,
    "count_as_violation": true,
    "violations_before_penalty": 2
  }
}
```

#### Параметры правил:
- `enabled` - включено ли правило
- `count_as_violation` - считать ли как нарушение или просто удалять сообщение
- `violations_before_penalty` - через сколько случаев записывать одно нарушение
  - Пример для `violations_before_penalty: 2`:
    * 1-е сообщение - базовое (не считается)
    * 2-е сообщение - первый случай
    * 3-е сообщение - второй случай = первое нарушение записано
    * 4-е сообщение - третий случай
    * 5-е сообщение - четвертый случай = второе нарушение записано
  - То есть, при значении 2, каждое второе нарушение правила записывается как одно фактическое нарушение

#### Временные рамки проверок:
Все проверки используют общий параметр `reply_cooldown_seconds`. Если установлено значение 5 секунд:

- `no_reply`: 
  * Первое сообщение можно отправить в любой момент
  * Следующее сообщение без реплая можно отправить через 5 секунд
  * Если отправить раньше - считается нарушением

- `double_reply`:
  * Первый ответ на сообщение можно отправить в любой момент
  * Повторный ответ на то же сообщение можно отправить через 5 секунд
  * Если ответить повторно раньше - считается нарушением

- `self_reply`:
  * После отправки своего сообщения нужно подождать 5 секунд
  * Только после этого можно отвечать на своё сообщение
  * Если ответить раньше - считается нарушением

### Настройка наказаний:

```json
"penalties": {
    "1": "warning",
    "2": "read-only",
    "3": "kick",
    "4": "kick+ban",
    "5": "ban"
}
```

Ключи - это количество нарушений, при котором применяется наказание. Значения - тип наказания.
Можно настроить несколько одинаковых наказаний для разных порогов, например:

```json
"penalties": {
    "2": "warning",
    "4": "warning",
    "6": "read-only",
    "8": "kick",
    "10": "ban"
}
```

В этом случае пользователь получит:
- Первое предупреждение на 2 нарушения
- Второе предупреждение на 4 нарушения
- Мут на 6 нарушений
- Кик на 8 нарушений
- Бан на 10 нарушений

## Требования 📋

- Python 3.7+
- SQLite3
- aiogram 3.x (последняя версия)
- pytz

## Установка 🛠

1. Клонируйте репозиторий:
```bash
git clone https://github.com/DiastroniX/dv_msg_inspector.git
cd dv_msg_inspector
```

2. Создайте виртуальное окружение и активируйте его:
```bash
python -m venv venv
source venv/bin/activate  # для Linux/Mac
# или
.\venv\Scripts\activate  # для Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл конфигурации `config.json` на основе `config.example.json`

5. Запустите бота:
```bash
python main.py
```

### Возможные проблемы при установке 🔧

1. Если у вас возникают проблемы с зависимостями:
   ```bash
   pip install -r requirements.txt --upgrade --force-reinstall
   ```

2. Если бот не запускается из-за ошибок совместимости:
   ```bash
   pip install aiogram --upgrade
   pip install -r requirements.txt --force-reinstall
   ```

## Разработка

### Запуск тестов

```bash
pytest tests/
```

### Создание релиза

1. Создайте новый релиз в разделе "Releases" на GitHub
2. Укажите версию (например, v1.0.0)
3. Добавьте описание изменений
4. Нажмите "Publish release"

После публикации релиза автоматически запустится процесс деплоя на сервер.

## Лицензия 📄

[MIT License](LICENSE)

### Работа с тредами в админ-чате 🧵

Бот использует два ключевых параметра для определения где работать и куда отправлять уведомления:

- `allowed_groups` - список ID групп, где бот проверяет сообщения на нарушения
- `admin_chat_id` - ID чата или треда, куда отправляются уведомления администраторам

При этом:
- Бот игнорирует все сообщения в админ-чате и его тредах (не проверяет на нарушения)
- В `admin_chat_id` можно указать как ID основного чата, так и ID конкретного треда - бот будет отправлять уведомления туда
- Группы из `allowed_groups` и чат из `admin_chat_id` могут быть никак не связаны между собой

#### Как получить ID треда (топика) 🔍

Треды в Telegram могут иметь ID в двух форматах:

1. Составной ID (рекомендуется):
   ```
   {chat_id}_{message_thread_id}
   ```
   Например: `-1002465953388_128`

2. Раздельные ID (через @getidsbot):
   1. Добавьте в группу бота [@getidsbot](https://t.me/getidsbot)
   2. Перейдите в нужный тред
   3. Отправьте любое сообщение
   4. Бот покажет:
      - `chat_id` - ID основной группы (начинается с -100)
      - `message_thread_id` - ID треда (обычное число)
   5. Соедините их через подчеркивание: `chat_id_message_thread_id`

Пример ответа от @getidsbot:
```
@YourUsername, message details:
message_id = 123456
from_id = 123456789
chat_id = -1001234567890
message_thread_id = 123456 
date = 2024-03-21 12:34:56
```
В этом случае ID треда будет: `-1001234567890_123456`

В `admin_chat_id` можно использовать любой из этих форматов.

Это позволяет гибко настроить работу бота:
- Проверять сообщения в одних группах
- Отправлять уведомления в совершенно другой чат/тред
- При этом не беспокоясь о том, что бот будет проверять сообщения в админ-чате 